name: Cron Test (Chỉ chạy khi ở main)

on:
  schedule:
    - cron: '* * * * *'  # Chạy mỗi 1 phút

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: In ra thông báo
        run: echo "Cron đang chạy ngon lành!"
